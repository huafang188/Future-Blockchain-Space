{
  "name": "fbs-worker",
  "version": "3.4.0",
  "private": true,
  "description": "Future Space Cloudflare Worker（含 EVM 签名验证）",
  "main": "worker.js",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "dependencies": {
    "@noble/secp256k1": "^2.2.3",
    "@noble/hashes": "^1.6.0"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
