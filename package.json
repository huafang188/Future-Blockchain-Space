{
  "name": "future-blockchain-space",
  "version": "1.0.0",
  "description": "NEO Mining Matrix",
  "scripts": {
    "build:css": "tailwindcss -i ./input.css -o ./tailwind.css --minify"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  }
}